node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# AI Studio generates this with real project identifiers — keep it local
firebase-applet-config.json

# Generated script briefs — local working output
exports/

# Personal prompt library for a separate project — local only
cyberpipeline-prompts.md

# Script-run checkpoints (server/runJournal.ts) — local, disposable
.runs/
